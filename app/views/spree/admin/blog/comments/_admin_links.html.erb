<%= link_to(
        t(:delete, scope: 'blog.admin.comments'), admin_blog_post_comment_path(post, comment),
        method: :delete,
        data: { confirm: t(:are_you_sure_you_want_to_remove_this_comment, scope: 'blog.admin.comments', default: "Are you sure?") }
    ) %>
