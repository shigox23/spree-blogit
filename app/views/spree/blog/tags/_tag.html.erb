<% if @is_head == true %>
  <%= link_to tag.upcase.gsub('-',' '), tagged_blog_posts_path(tag), class: "blog-main-tag" %>
<% else  %>
  <%= link_to tag.gsub('-',' '), tagged_blog_posts_path(tag), class: "blog_tag__link" %>
<% end %>
